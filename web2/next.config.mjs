/** @type {import('next').NextConfig} */

const nextConfig = {
    images:{
        domains: ["i.pinimg.com", "img.icons8.com", "res.cloudinary.com", "gateway.pinata.cloud","via.placeholder.com"]
    },
    reactStrictMode: false, // Disable Strict Mode

};

export default nextConfig;
